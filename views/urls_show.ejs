<DOCTYPE html>
<html lang="en">
  <head>
  </head>
<body>
  <% include partials/_header %>
  <% for (let shortUrl in urls) { %>
  <% let longUrl = urls[shortUrl]; %>
  <a href="<%= shortUrl%>"><%= longUrl%></a>
</body>
</html>
